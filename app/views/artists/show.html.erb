<h1><%= @artist.name %></h1>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= song.title %></li>

    <!--  specfic song by an artist, using song#show -->
    <li><%= link_to song.title, artist_song_path(song.artist, song) %></li>
  <% end %>
</ul>
